/**
 * Validation middleware alias.
 * It gives modules a clean import path while preserving validate-request.middleware.js compatibility.
 */
export { validateRequest } from "./validate-request.middleware.js";
